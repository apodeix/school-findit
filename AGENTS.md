# 《어디 있니?》 프로젝트 개발 지침

이 문서는 Codex가 새 세션에서도 프로젝트 목적, 합의된 기능, 권한 구조,
디자인 방향과 개발 우선순위를 유지하도록 제공하는 프로젝트 지침이다.

## 프로젝트 개요

- 서비스명: 어디 있니?
- 부제: 우리 학교 분실물 찾기
- 영어 프로젝트명: School Findit
- 권장 폴더·저장소명: school-findit
- 형태: 중학교 구성원이 사용하는 반응형 웹앱
- 기획자는 중학교 교사이며 전문 개발자가 아니다. 결과와 선택지는 쉬운
  한국어로 설명한다.
- 교사 코딩 연수에서 약 6시간 안에 시연 가능한 MVP를 만든 뒤 실제 학교
  사용 수준으로 확장한다.

## 해결하려는 문제

분실물 관리 담당 교사가 습득 물품을 메신저로 담임 교사에게 알리고,
담임 교사가 학생들에게 다시 안내하는 과정이 여러 단계를 거쳐 번거롭다.
학생들은 물건을 찾기 위해 여러 교무실을 방문하거나 반복해서 문의한다.
다른 학생이 물건을 보았어도 알려줄 통로가 없어 유용한 정보가 사라지고,
습득물이 장기간 보관되어 처리가 곤란한 경우도 생긴다.

이 웹앱은 한곳에서 분실 신고, 습득물 확인, 목격 단서 공유, 보관 장소
확인과 반환 상태 확인을 할 수 있게 한다.

## 확정된 범위와 제약

- 초기 버전은 한 학교 전용이다. 다중 학교 기능은 만들지 않는다.
- 학교 Google Workspace 계정 로그인을 사용할 예정이다.
- 로그인한 신규 사용자의 기본 권한은 학생이다.
- 외부 API는 가능하지만 유료 인공지능 API는 사용하지 않는다.
- AI 기능은 핵심 기능이 아니며 임의로 추가하지 않는다.
- 모바일 앱이 아니라 웹앱이다.
- MVP 알림은 앱 내부 알림이다. 웹 푸시는 후속 기능으로 둔다.
- 장소는 선택 목록으로 제한하지 않고 자유롭게 입력한다.
- 찾기 단서 작성 횟수를 제한하지 않는다.
- 학생이나 교사가 별도의 확인번호를 기억하게 만들지 않는다.
- 공개 화면에 얼굴, 이름표, 학생증 정보, 전화번호를 노출하지 않는다.
- 다른 학생에게 작성자의 실명과 이메일을 표시하지 않는다.

## 사용자 역할

### 학생

- 공개된 분실 신고와 습득물을 검색·열람한다.
- 본인의 분실 신고를 작성·수정·종료한다.
- 습득한 물건을 사진과 설명으로 등록한다.
- 활성 분실 신고에 찾기 단서를 작성한다.
- 본인이 작성한 단서만 수정·삭제한다.
- 본인 글과 관련된 앱 내부 알림을 확인한다.

### 일반 교사

- 학생 기능을 모두 사용할 수 있다.
- 직접 습득물을 등록하면 즉시 공개할 수 있다.
- 학생이 등록한 습득물을 실제로 전달받은 뒤 교사 인수 처리한다.
- 보관 장소를 입력하고 반환 여부를 처리한다.
- 부적절한 게시물이나 단서를 임시 숨김 처리할 수 있다.

### 최종 관리자 교사

- 일반 교사 기능을 모두 사용할 수 있다.
- 모든 게시물과 단서를 관리하거나 삭제한다.
- 교사 인증코드를 변경하고 일반 교사 권한을 회수한다.
- 다른 교사를 최종 관리자로 승격한다.
- 최초 관리자는 분실물 업무 담당자의 학교 계정을 미리 지정한다.

## 교사 권한 부여

1. 사용자는 학교 Google 계정으로 로그인한다.
2. 신규 사용자는 학생 권한으로 시작한다.
3. 교직원 메신저로 공유된 교사 인증코드를 계정 설정에서 한 번 입력한다.
4. 서버가 코드를 검증한 뒤 일반 교사 권한을 부여한다.
5. 인증 후에는 코드를 다시 입력하지 않는다.
6. 최종 관리자만 인증코드를 변경할 수 있다.

보안 원칙:

- 클라이언트가 자신의 역할을 직접 변경할 수 없어야 한다.
- 인증코드를 원문 그대로 공개 데이터베이스에 저장하지 않는다.
- 코드 검증과 역할 변경은 서버 측에서 처리한다.
- 최종 관리자 권한은 기존 최종 관리자만 부여한다.
- 허용된 학교 이메일 도메인이 아닌 계정은 차단한다.

## 핵심 업무 흐름

### 분실 신고

1. 학생 또는 교사가 물건명, 사진, 날짜, 마지막으로 본 장소와 특징을
   입력한다.
2. 상태는 찾는 중으로 시작한다.
3. 다른 구성원이 목격 정보를 찾기 단서로 남긴다.
4. 작성자가 찾았어요로 변경한 뒤 종료한다.
5. 종료된 신고에는 새 단서를 작성할 수 없다.

상태: 찾는 중 → 찾았어요 → 종료

### 학생이 습득물을 등록한 경우

1. 학생이 사진과 습득 장소·날짜·특징을 등록한다.
2. 전달 대기 상태이며 아직 공개 목록에 노출하지 않는다.
3. 학생이 물건을 실제 교사에게 전달한다.
4. 교사가 인수를 확인하고 보관 장소를 입력한다.
5. 교사 인수 또는 주인 찾는 중 상태가 되면서 공개된다.
6. 주인에게 전달하면 반환 완료로 변경한다.

상태: 전달 대기 → 교사 인수 → 주인 찾는 중 → 반환 완료

교사가 습득물을 직접 등록하는 경우 이미 물건을 보관하고 있으므로 보관
장소를 입력하고 즉시 공개할 수 있다.

## 찾기 단서

일반 댓글이 아니라 분실물의 위치를 찾기 위한 목격 정보다.

- 본 장소: 자유 입력
- 본 날짜
- 대략적인 시간
- 이동 방향이나 상황 등 상세 설명
- 학교 계정 사용자만 작성
- 활성 분실 신고에만 작성
- 횟수 제한 없음
- 작성자는 자신의 단서만 수정·삭제
- 전화번호와 SNS 계정을 적지 않도록 안내
- 부적절한 단서는 신고 가능
- 교사는 임시 숨김, 최종 관리자는 삭제 가능

## 알림

MVP에서는 앱 내부 알림함과 읽지 않은 개수 배지를 제공한다. 알림을 누르면
관련 물건 상세 화면으로 이동한다.

- 내 분실 신고에 새 찾기 단서가 등록됨
- 내 신고와 비슷한 습득물이 공개됨
- 학생이 등록한 습득물을 교사가 인수함
- 보관 장소가 입력 또는 변경됨
- 게시물 상태가 변경됨

유사 물건 알림은 AI 없이 분류, 색상, 날짜, 장소 키워드의 단순 규칙으로
구현한다.

## 도움 포인트와 배지

도움 포인트는 학생 간 경쟁이나 순위를 위한 기능이 아니라 확인된 선행과 학교
공동체 기여를 격려하기 위한 기능이다. 학생 간 공개 순위표는 만들지 않는다.

포인트 지급 기준:

- 학생이 등록한 습득물을 교사가 실제로 전달받고 인수 확인하면 등록 학생에게
  3점을 지급한다.
- 분실 신고 작성자가 찾기 단서의 `도움 됐어요`를 선택하면 단서 작성자에게
  1점을 지급한다.
- 같은 학생이 같은 분실 신고에 여러 단서를 남겨도 단서 포인트는 최대 한 번만
  지급한다.
- 같은 습득물 인수 또는 같은 단서 활동에 중복 지급하지 않는다.
- 허위 또는 장난 제보로 확인되면 교사나 최종 관리자가 지급을 취소하되 거래
  기록은 삭제하지 않는다.

배지 기준은 `lib/rewards.ts`의 별도 상수로 관리하며 추후 설정값으로 옮길 수
있게 한다.

- 첫 번째 도움: 최초 도움 포인트 획득
- 분실물 해결사: 유용한 찾기 단서를 기준 횟수 이상 제공
- 우리 학교 지킴이: 습득물을 교사에게 기준 횟수 이상 전달

포인트 총점과 활동 내역은 본인, 담당 교사와 최종 관리자만 확인한다. 공개
화면에는 실명이나 총점을 표시하지 않으며 필요한 경우 익명 배지만 표시한다.

데이터 및 보안 원칙:

- 총점만 저장하지 않고 사용자, 점수, 지급 사유, 관련 물건·단서 ID, 지급 시각,
  처리 교사와 취소 정보를 포함하는 거래 내역을 저장한다.
- 포인트 지급과 취소는 서버 또는 Cloud Function에서 권한과 중복 키를 확인한
  뒤 처리한다. 클라이언트가 직접 거래를 생성하거나 수정할 수 없게 한다.
- 습득물 인수는 물건 ID, 단서 도움은 `신고 ID + 단서 작성자 ID`를 고유 조건으로
  사용해 중복 지급을 막는다.

## 검색과 필터

- 전체 / 잃어버렸어요 / 주인을 찾아요
- 물건 이름, 분류, 색상
- 날짜 또는 최근 7일
- 장소 키워드
- 진행 중인 글만 보기
- 최신순 정렬

장소는 자유 입력이지만 문자열 일부가 일치하면 검색할 수 있어야 한다.

## 개인정보와 안전

- 사진 등록 단계에서 얼굴, 이름표, 학생증 정보, 전화번호를 가리도록
  안내한다.
- 작성자의 이메일과 실명을 공개하지 않는다.
- 학생증 사진은 개인정보 부분을 가린 경우에만 허용한다.
- 관리자도 업무에 필요한 최소한의 계정 식별 정보만 확인한다.
- 신고, 숨김, 삭제 기록을 남긴다.
- 소유권 확인용 세부 특징을 공개 글에 모두 노출하지 않는 방식을 고려한다.

## 디자인 원칙

Material Design 3를 디자인 규칙으로 사용하되 Material Web 패키지에
강하게 종속되지는 않는다.

- 선명한 인디고를 주색으로 사용한다.
- 행정 시스템보다 친근하고 세련된 학교 생활 도구처럼 보이게 한다.
- 둥근 카드, 상태 칩, 큰 검색창과 충분한 터치 영역을 사용한다.
- 상태는 색상과 문구로 동시에 구분한다.
- 모바일은 하단 내비게이션과 큰 등록 버튼을 사용한다.
- 데스크톱은 좌측 메뉴, 가운데 검색·목록, 우측 최근 단서 구조다.
- 첫 화면에서 홍보 문구보다 검색과 물건 목록이 바로 보여야 한다.
- 본문은 기본 16px 이상, 반복 라벨은 14px 이상을 권장한다.
- 모바일과 데스크톱 모두 글자 잘림과 가로 스크롤이 없어야 한다.
- 접근 가능한 대비, 키보드 포커스와 의미 있는 버튼 라벨을 유지한다.

## 현재 기술과 구현 상태

### 2026-09-13 연결 작업 중간 기록

- 운영 화면은 `app/findit-app.tsx`, 입력 폼은 `app/findit-forms.tsx`로 분리했다.
- `api/app.ts` Vercel 서버가 Firebase 로그인 토큰, 실제 역할, 소유권과 중복 지급을 검증한다.
- 실제 단서 CRUD·신고·숨김, 인수·반환·보관 장소, 포인트 거래·취소와 사건별 알림을 연결했다.
- 시연 화면과 로그인 후 실제 데이터 화면은 분리한다. 로그인 후 예시 자료를 채우지 않는다.
- Firebase Admin 비공개 키는 Vercel Production 시크릿에만 등록했다. 소스·대화에 노출하지 않는다.
- `firestore.server.rules`는 데이터의 직접 읽기·쓰기를 차단한다. API 운영 확인 후 배포한다.
- 학생과 교사 모두 하단 찾기에서 검색할 수 있고, 교사는 학생 기능에 업무 기능이 추가된다.
- 입력·권한·중복 방지 단위 검사 8건과 실제 저장소 통합 검사 26건을 통과했다.
- Windows Node 24 빌드는 산출 후 종료 assertion이 발생한다. Vercel 빌드와 API 실행을 별도 확인한다.
- 인증코드는 새 설정 시 12~32자 복합 문자를 요구하고 scrypt와 임의 salt로 저장한다. 이전 SHA-256 코드는 새 인증에 사용하지 않으며 최종 관리자가 새 코드를 설정해야 한다. 기존 인증 교사의 역할은 유지한다.
- 로그인 전에는 별도 `demo-preview.tsx`의 시연 화면을 표시하고, 로그인 후에는 실제 자료만 표시한다. 로고와 파비콘은 살짝 웃는 돋보기로 통일했다.
- Vercel에서 Firebase Admin 14의 ESM 의존성 오류가 확인되어 13.6 계열과 Firestore 7 계열을 사용한다.
- 학교 도메인 미확정으로 Gmail 시험 허용은 유지한다.

현재 프로젝트는 Next.js/Vinext, React, TypeScript, Tailwind CSS,
Shadcn 계열 컴포넌트와 Lucide 아이콘을 사용한다. pnpm을 유지한다.

구현된 기능:

- Material 3 기반 반응형 메인 화면
- 검색어 목록 필터링과 분실·습득 필터
- 물건 카드와 상세 대화상자
- 앱 내부 알림 대화상자
- 분실·습득 등록 대화상자
- 시연용 항목을 현재 화면 목록에 추가
- 모바일 하단 메뉴와 데스크톱 좌·우 패널
- Firebase Google 로그인과 사용자 프로필 생성
- Firestore 물건 등록과 공개 목록 실시간 조회
- 계정 역할에 따른 학생·교사·최종 관리자 메뉴 구분
- 사진 선택·카메라 촬영·미리보기·자동 축소와 Firestore 영구 저장
- 교사·최종 관리자의 실제 전달 대기 습득물 조회, 보관 장소 입력과 인수 공개
- 최종 관리자의 교사 인증코드 설정과 학생 계정의 1회 교사 인증
- 교사 인수 알림의 Firestore 저장, 읽지 않음 표시와 최종 관리자 취소 기록
- 내가 쓴 글 실제 필터와 내가 남긴 단서 화면 전환
- 날짜 입력, 선택 항목 표시와 입력칸별 오류 안내
- 본인의 진행 중 글 수정과 확인 후 소프트 삭제, 최종 관리자 소프트 삭제
- 도움 포인트 총점·배지·활동 내역 시연 화면
- 신고 작성자의 `도움 됐어요` 및 동일 학생 중복 지급 방지 시연
- 교사 인수 확인과 3점 지급, 관리자 지급 취소 시연

아직 연결되지 않은 기능:

- 학교 Google Workspace 도메인 제한(현재는 역할 시험용 Gmail 임시 허용)
- 대용량 사진 저장소 전환(현재는 결제수단 없는 Firestore 압축 저장)
- 모든 작업에 대한 실제 사용자별 소유권 및 역할 검증
- 교사 인증코드 시도 횟수 제한과 교사 권한 회수
- 최종 관리자 승격 등 전체 관리자 권한 변경
- 실제 찾기 단서 CRUD 및 신고
- 실제 알림 생성과 읽음 처리
- 교사 인수 외 나머지 사건의 실제 알림 생성
- 도움 포인트 거래의 서버 지급·취소와 Firebase 영구 저장

로그인 사용자의 새 분실 신고는 Firestore에 영구 저장된다. 학생이 등록한
습득물은 전달 대기 상태라 공개 목록에 나오지 않는다. 기존 예시 목록과 단서,
도움 포인트, 인수 대기·관리자 화면은 아직 시연 데이터이므로 완성된 기능이나
영구 저장이라고 표현하지 않는다.

## 확정된 백엔드 방향

2026-09-13 사용자 결정: 실제 학교 사용을 위한 백엔드는 Firebase로 연결한다.
Firebase 프로젝트와 웹 앱, 서울 리전 Firestore 데이터베이스, 기본 보안 규칙은
준비되었다. Firebase 소유자 계정 하나를 임시 허용 계정과 최초 관리자로 지정한
비공개 설정도 생성했다. Google 로그인 제공자와 Vercel 승인 도메인은 활성화했다.
여러 계정으로 역할을 시험하기 위해 검증된 Gmail 계정을 임시 허용하며 새 계정은
학생으로 생성한다. 학교 도메인 전환과 실제 기능별 영구 저장 연결은 아직 필요하다.

Vercel 운영 주소는 `https://school-findit.vercel.app`이며 Vinext의 정적 내보내기
결과인 `dist/client`를 배포한다. Firebase 클라이언트 환경변수는 Vercel의
Production, Preview, Development 환경에 등록되어 있다.

- Firebase Authentication: 학교 Google 계정 로그인
- Cloud Firestore: 사용자, 물건, 찾기 단서, 알림과 상태 기록
- Firebase Storage: 물건 사진
- 서버 또는 Cloud Function: 인증코드 검증, 역할 변경, 관리자 작업

물건 데이터와 사진 저장에 D1/R2를 중복 연결하지 않는다. 웹앱의 배포 위치는
백엔드 선택과 별도로 결정한다. 외부 자격 증명이 필요하면 추측하거나 가짜
값을 넣지 말고 사용자에게 요청한다.

Firebase Storage는 현재 Blaze 요금제 및 결제 계정 연결이 필요하다.
무료 사용량이 있어도 전체 서비스를 완전 무료라고 안내하지 않는다.
결제 계정 연결과 유료 요금제 전환은 사용자의 명시적 승인 없이 실행하지 않는다.
공식 확인 자료: https://firebase.google.com/docs/storage/faqs-storage-changes-announced-sept-2024

2026-09-13 사용자 결정: Blaze로 업그레이드하지 않고 사진을 JPEG 데이터 URL로
압축해 물건 Firestore 문서에 저장한다. 이미지 문자열은 450,000자 이하로 제한하고
긴 변 기준 1,000px 이하로 축소한다. 이 방식은 소규모 시험 운영용이며 목록 읽기
비용과 문서 크기가 커지므로 이용량 증가 시 전용 객체 저장소로 이전해야 한다.

운영 연결 전에 확인할 정보:

- 허용할 학교 이메일 도메인
- 최초 관리자 교사의 학교 계정(공개 소스에 기록하지 않음)
- 사진 저장을 위한 Blaze 요금제 사용 여부

서비스 계정 비공개 키나 계정 비밀번호를 대화에 붙여넣도록 요청하지 않는다.
로그인·소유권·학교 접근 제한을 영구 저장과 함께 설계하고 검증한다.

예상 데이터 구조:

- users
- items
- clues
- notifications
- reports
- role_audit_logs
- reward_transactions
- app_settings

## 개발 순서

1. 현재 화면을 실행하고 모바일·데스크톱 오류를 확인한다.
2. 분실 신고, 습득물 등록과 찾기 단서 입력 흐름을 완성한다.
3. 배포 환경을 결정하고 데이터 모델과 보안 규칙을 확정한다.
4. 학교 Google 로그인, 학교 도메인 제한과 최초 관리자를 설정한다.
5. 인증코드 기반 교사 권한과 역할별 서버 검증을 구현한다.
6. 물건, 단서, 알림과 사진을 Firebase 영구 저장소에 연결하고 접근 권한을 검증한다.
7. 인수 대기, 보관 장소, 반환 완료와 관리자 기능을 구현한다.
8. 로딩, 빈 목록, 오류, 저장 성공, 재로그인과 권한 우회를 검증한다.

## 개발 작업 규칙

- 기능을 임의로 크게 늘리거나 유료 AI 서비스를 추가하지 않는다.
- 사용자 요청은 가능한 경우 직접 구현하고 실행 결과까지 확인한다.
- 권한은 화면 숨김만으로 처리하지 말고 서버에서도 검증한다.
- 인증정보, API 키와 인증코드를 소스에 직접 작성하거나 커밋하지 않는다.
- 기존 Material 3 디자인과 반응형 구조를 유지한다.
- 새 UI는 기존 색상 토큰과 둥근 형태를 재사용한다.
- 실제 사진이 없으면 깨진 이미지나 임의의 외부 URL을 넣지 않는다.
- 현재 의존성으로 가능한지 확인한 뒤에만 새 패키지를 추가한다.
- 패키지 관리자를 pnpm에서 임의로 바꾸지 않는다.
- 변경 후 TypeScript 검사와 프로덕션 빌드를 실행한다.
- 실제 학생이나 교사의 개인정보를 샘플 데이터에 넣지 않는다.
- 미결정 사항이나 자격 증명이 필요하면 한 번에 이해하기 쉽게 질문한다.

## 기본 명령

- 의존성 설치: pnpm install
- 개발 화면 실행: pnpm dev
- 타입 검사: pnpm exec tsc --noEmit
- 프로덕션 빌드: pnpm build

환경 차이로 명령이 실패하면 패키지 관리자나 구조를 바로 교체하지 말고
오류 원인을 먼저 확인한다.

## 다음 Codex 세션의 첫 작업

1. 이 파일과 현재 소스 구조를 읽는다.
2. 현재 화면을 실행해 구현 상태를 확인한다.
3. 사용자가 원하는 배포 환경을 확인한다.
4. 확정된 Firebase 방향에 따라 프로젝트 설정과 학교 계정 정보를 확인한다.
5. 데이터 모델과 보안 규칙을 확정한다.
6. 실제 물건 등록과 목록 조회를 영구 저장소에 연결한다.

첫 요청 예시:

“AGENTS.md를 먼저 읽고 프로젝트의 현재 상태를 확인해줘. 화면을 실행한
뒤 다음 개발 단계와 선택이 필요한 사항을 쉬운 한국어로 설명해줘.”
